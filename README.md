# Spring JPA Paging + Jackson View

This projects shows a solution using jackson mix-ins to make the
`Page<T>` interface from spring jpa work with `@JsonView`.

## Running

### App

`gradle bootRun`

### Test

`gradle test`